# 시연 모드(Supabase)로 돌릴 때만 채웁니다. 값은 Supabase > Project Settings > API 에서 복사.
# service_role 키는 절대 넣지 않습니다.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# 이 줄이 supabase 일 때만 데이터를 Supabase에 저장합니다. 비워 두면 "내 PC 모드"(이 컴퓨터에만 저장).
NEXT_PUBLIC_STORAGE=
