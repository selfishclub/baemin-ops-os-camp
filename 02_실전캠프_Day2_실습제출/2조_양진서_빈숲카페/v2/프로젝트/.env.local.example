# 이 파일을 복사해 .env.local 로 만들고 = 뒤에 값을 붙여넣습니다. .env.local 은 GitHub에 올라가지 않습니다.
# Supabase 대시보드 → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# service_role 키는 절대 여기에도, 어디에도 적지 않습니다.
