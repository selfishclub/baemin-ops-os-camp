# v2 제출서 — 빈숲카페 / 양진서

> 진행 중 — 10/2 1차 마감까지 계속 채웁니다

## 만든 것

- 도구 이름: 빈숲 레시피OS (레시피북 v2)
- 한 줄 설명: v1 레시피북에 직원 로그인·사장 편집·읽음 확인·빈숲OS 알림을 더하는 버전. 첫 작업으로 단면도를 재료 색으로 자동 표시
- 프로젝트 폴더: `v2/프로젝트/` (v1/프로젝트를 복사해 이어 만듦. 코드·프롬프트 기록·이미지·문서 전부. 10/2까지 여기)
- 실행 방법:
  1. `v2/프로젝트` 폴더에서 `npm install` (Node 22 이상)
  2. `npm run dev`
  3. 브라우저에서 http://localhost:3000 열기
- 첫 화면 스크린샷: `결과물/첫화면.png` (구경하기 표에 올라갑니다)
- 다른 스크린샷: (추가 예정)
- 영상이 있으면 링크로 (파일은 올리지 않습니다): 없음
- PRD 아티팩트 링크 (사전 과제로 만든 웹페이지, 있으면): 없음
- Vercel 배포 주소 (오늘은 있으면. 만드는 법 → [부록](../../../03_인터넷에서_접근가능하게_만들기/부록_CLI로_바로_올리기.md)): 아직 없음 — 실제 레시피는 로그인 뒤에만 보이게 만든 다음에 올립니다

## 이번 버전에서 되는 것 (9/17 기준)

- 단면도 색 자동 지정: 층 이름에 딸기·말차·초코·카라멜·자몽·레몬·블루베리·민트·홍차 등 재료가 들어가면 16가지 팔레트에서 색이 자동으로 정해짐. 편집할 때 목록에서 바꿀 수 있고, 목록에 없는 재료는 "직접 고른 색"으로 지정
- 얼음층에 얼음 조각 표시 (튀지 않게 연한 파란 테두리)
- 단면도는 잔 아래(1층)부터 넣는 순서대로 쌓이고, 층마다 번호 표시. 관리자 화면 입력 설명도 "첫 줄이 맨 아래"로 통일
- 관리자 화면 텍스트 입력에서 색상 칸을 비우면 이름으로 자동, `#색상값`도 가능
- v1 기능 전부 (검색·상세·최종 수정일·영상 프롬프트 생성)

## 아직 안 되는 것 / 다음에 할 것

- 직원 로그인·권한 + 사장 편집 화면 (지금은 ChatGPT 계정 연동이라 이 저장소에서 편집 모드가 열리지 않음) → 꼭 할 것 1
- 바뀐 레시피 읽음 확인 + 빈숲OS 알림 → 꼭 할 것 2
- 신입 체크리스트·퀴즈, 영상·사진 워터마크, 챗봇 → 여유 있으면 (`PRD.md` 참고)

## 한 줄 소감

- 

## 10/2부터 — 개인 저장소로 옮긴 뒤 적습니다 (그때부터 필수)

- 내 GitHub 저장소 주소 (Public):
- Vercel 배포 주소 (GitHub 연동):

---
제출 전 확인: [x] 만든 폴더가 내 폴더 `프로젝트/` 안에 통째로 있나 [x] 그 안에 `.git` 폴더가 없나 [x] `.env`·키·`node_modules`가 올라가지 않았나 [x] `결과물/첫화면.png`를 넣었나 [ ] 이 파일을 커밋·push 했나
